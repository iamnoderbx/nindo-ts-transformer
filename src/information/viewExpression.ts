import ts from "typescript";
import { TransformContext } from "../transformer";

export default function viewExpression(context: TransformContext, expression: ts.Expression) {
    // No longer needed- was used to view expressions.
}